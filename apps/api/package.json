{
  "name": "kareo-api",
  "version": "0.0.1",
  "private": true,
  "description": "Kareo Backend API Foundation (TASK-B-002) — Node.js + TypeScript, Netlify Functions style, Supabase (PostgreSQL + Auth) as data layer only.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.1.1",
    "@types/node": "^22.5.5"
  }
}
